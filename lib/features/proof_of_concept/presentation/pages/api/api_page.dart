import 'package:flutter/material.dart';

/// Page showing the results of specific Api calls on the Finary API
class ApiPage extends StatelessWidget {
  /// Constructor for the [ApiPage]
  const ApiPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ApiPage'),
      ),
    );
  }
}
